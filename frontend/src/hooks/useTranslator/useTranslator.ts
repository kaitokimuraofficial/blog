// /* Library */
// import { useMemo } from 'react';

// import { jp } from '@/locale/jp';

// interface Params {
//   [key: string]: any;
// }

// function translatorFunc(page: string): (key: string) => string {
//   const lang = 'jp'

//   const pp = (key: string) => {
//     return "Hello"
//   }

//   return pp
// }

// export function useTranslator(page: string) {
//   const translator = useMemo(() => translatorFunc(page), [page]);
//   return translator;
// }
